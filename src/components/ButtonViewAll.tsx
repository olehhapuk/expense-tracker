import { styled, Button } from '@mui/material';

const ButtonViewAll = styled(Button)({
  color: '#A3A3A3',
  fontSize: '12px',
  fontWeight: 400,
  textTransform: 'none',
});

export default ButtonViewAll;
